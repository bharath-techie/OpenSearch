source = clickbench | where (like(Title, '%Google%') and not like(URL, '%.google.%') and SearchPhrase != '') and match(URL, 'http') | stats count() as c, dc(UserID) by SearchPhrase | sort - c | head 10
